<div class="stock-alert-bar-en">
  <span class="pulse-dot"></span>
  <span class="stock-text">Kun 6 varer tilbage på lager!!</span>
</div>

<style>
.stock-alert-bar-en {
  display: flex;
  align-items: center;
  background-color: #eeeeee;
  color: #333;
  padding: 10px 14px;
  font-size: 14px;
  border: 1px solid #ccc;
  border-radius: 6px;
  gap: 8px;
  width: fit-content;
  margin: 12px 0;
  animation: fadeIn 0.5s ease-in-out;
}

.pulse-dot {
  width: 10px;
  height: 10px;
  background-color: #e60000;
  border-radius: 50%;
  animation: pulse 1.2s infinite;
}

@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(230, 0, 0, 0.7); }
  70% { box-shadow: 0 0 0 8px rgba(230, 0, 0, 0); }
  100% { box-shadow: 0 0 0 0 rgba(230, 0, 0, 0); }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-5px); }
  to { opacity: 1; transform: translateY(0); }
}
</style>