{% comment %} 
  How to get poster image from video 
{% endcomment %}
{%- assign poster_image = section.settings.poster | default: section.settings.video.preview_image -%}
{%- if poster_image != blank -%}
  {{- poster_image | image_url: width: poster_image.width | image_tag: sizes: '100vw', widths: '200,300,400,500,600,700,800,900,1000,1200,1400,1600,1800,2000,2200,2400,2600,2800,3000,3200' -}}
{%- else -%}
  {{- 'lifestyle-1' | placeholder_svg_tag: 'h-full w-full placeholder' | replace: '<svg', '<svg preserveAspectRatio="xMinYMin slice"' -}}
{%- endif -%}


{% comment %} 
  How to get different placeholder svg images
{% endcomment %}
{% for i in (1..10) %}
  {% capture current %}{% cycle 1, 2, 3, 4, 5, 6 %}{% endcapture %}
  {{ 'product-' | append: current | placeholder_svg_tag: 'placeholder-svg' }}
{% endfor %}

{% comment %} 
  Get Product by handle
{% endcomment %}
{% assign some_product = all_products['some-handle'] %}
{% assign another_product = all_products.some_handle %}

{% comment %} 
  Get string to numeric value
{% endcomment %}
{% assign string_digit = "1" %} {% # all the schema type except number return string values %}
{% assign numeric_digit = string_digit | plus: 0  %}

{% comment %} 
  Get outter loop's index
{% endcomment %}
{% for i in (1..5) %}
  {% for j in (1..5) %}
    {% if forloop.parentloop.index == 2 %}
      echo "outter loop's index is 2"
    {% endif %}
  {% endfor %}
{% endfor %}

{% comment %} 
  Add currency iso code
{% endcomment %}
{{ cart.currency.iso_code }}

{% comment %} 
  Add collection url into product url 
{% endcomment %}

{{ product.url | within: collection }}

{% comment %} 
  Add first collection url into product url 
{% endcomment %}
{% if product.collections.size > 0 %}
  {% assign first_collection = product.collections.first %}
  <a href="{{ first_collection.url }}/products/{{ product.handle }}">{{ product.title }}</a>
{% else %}
  <a href="{{ product.url }}">{{ product.title }}</a>
{% endif %}

{% comment %} 
  Location / market based content login 
{% endcomment %}
{% if localization.market.handle == "duitsland" %}
  {% # Some content only for duitslan market %}
{% else %}
  {% # Contents for other markets %}
{% endif %}

{% comment %} 
  Check if a block is exist or not 
{% endcomment %}
{% assign popups = section.blocks | where: 'type', 'popup' %}

